# ========================================
# Spring Profiles 환경 설정
# ========================================
# 개발 환경: --spring.profiles.active=dev
#   - docker-compose.dev.yml 자동 실행 (MariaDB, Redis만)
#   - Spring Boot는 IntelliJ/Gradle로 직접 실행
#   - DB: localhost:3306/toy, Redis: localhost:6379
# 프로덕션: --spring.profiles.active=prod
#   - docker-compose.yml 실행 (Backend + 인프라)
#   - DB: mariadb:3306/jwt_auth (Docker 서비스명)
# ========================================

# Docker Hub (프로덕션 배포용)
DOCKER_USERNAME=your-dockerhub-username

# Database
# - dev profile: localhost:3306/toy 사용 (docker-compose.dev.yml)
# - prod profile: mariadb:3306/jwt_auth 사용 (docker-compose.yml)
DB_USERNAME=root
DB_PASSWORD=jwt_password

# OAuth2 Client - Google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# OAuth2 Client - Kakao
KAKAO_CLIENT_ID=your-kakao-client-id
KAKAO_CLIENT_SECRET=your-kakao-client-secret

# OAuth2 Client - Naver
NAVER_CLIENT_ID=your-naver-client-id
NAVER_CLIENT_SECRET=your-naver-client-secret

# OAuth2 Redirect URI (선택적 오버라이드)
# 기본값:
#   - dev:  http://localhost:5173/oauth/callback
#   - prod: http://localhost:3000/oauth/callback
# OAUTH2_REDIRECT_URI=http://your-custom-domain/oauth/callback

# JWT
JWT_SECRET=your-jwt-secret-key-at-least-256-bits
JWT_ACCESS_TOKEN_EXPIRATION=900000
JWT_REFRESH_TOKEN_EXPIRATION=604800000
